package org.apache.spark.sql.hive.customnativefunctions

import org.apache.spark.sql.catalyst.expressions.Expression
import org.apache.spark.sql.types.{DataType, StructType}

//case class AddFunc(name:String, left:Expression, right:Expression){
////  def  addleftStruct(): StructType ={
////    val l = left.asInstanceOf[StructType]
////    val r = right.asInstanceOf[StructType]
////    val lv = l.fields.filter(f => f.name.equalsIgnoreCase(name)).head
////
////  }
//
//  def addInt(a:Int, b: Int): Int = {
//    java.lang.Double.sum()
//    a+b
//  }
//
//
//}
